import { atlassianConnectDevTool } from './atlassian-connect-dev-tool';

describe('atlassianConnectDevTool', () => {
  it('should work', () => {
    expect(atlassianConnectDevTool()).toEqual('atlassian-connect-dev-tool');
  });
});
